import React, { forwardRef } from 'react'

const  QuillEditor =  forwardRef(({ value, onChange, placeholder, className, height = 400 }) =>  {
  return (
    <div>
      
    </div>
  )
})

export default  QuillEditor
